# AI 代理協助翻譯指南 (AI Agents Translation Guide)

本文件說明如何使用 AI 代理來協助 Python 官方說明文件臺灣繁體中文翻譯計畫的翻譯工作。

## 目錄

- [概述](#概述)
- [使用 AI 代理的優勢](#使用-ai-代理的優勢)
- [AI 代理翻譯流程](#ai-代理翻譯流程)
- [翻譯品質控制](#翻譯品質控制)
- [注意事項與限制](#注意事項與限制)
- [最佳實踐](#最佳實踐)

## 概述

AI 代理可以協助加速 Python 文件的繁體中文翻譯工作，但仍需要人工審核與校對以確保翻譯品質。本指南將說明如何有效運用 AI 代理進行翻譯。

## 使用 AI 代理的優勢

1. **提高效率**：快速處理大量文字翻譯
2. **一致性**：保持術語翻譯的統一性
3. **格式保持**：自動保留 reStructuredText 格式
4. **初稿產生**：提供高品質的翻譯初稿

## AI 代理翻譯流程

### 1. 準備工作

確保您已經：
- Fork 並 clone 了專案
- 安裝了必要的工具（uv、gettext、pre-commit）
- 熟悉基本的翻譯流程（參考 README.rst）

### 2. 選擇翻譯檔案

AI 代理可以使用以下指令來自動分析和選擇翻譯目標：

#### 分析翻譯狀態的指令

1. **查看整體翻譯進度**：
   ```bash
   make progress
   ```
   顯示每個 po 檔的翻譯完成度百分比，幫助 AI 代理選擇優先翻譯目標。

2. **列出待翻譯條目**：
   ```bash
   make todo
   ```
   列出所有尚未翻譯的 msgstr 條目，AI 代理可以據此決定工作重點。

3. **找出需要校閱的條目**：
   ```bash
   make fuzzy
   ```
   列出標記為 `#, fuzzy` 的條目，這些是已翻譯但需要更新的內容。

#### 自動選擇翻譯目標的策略

AI 代理應按以下優先級選擇翻譯目標：

1. **高優先級**：
   - Tutorial 目錄下翻譯完成度 < 80% 的檔案
   - 有 fuzzy entries 的檔案（使用 `make fuzzy` 檢查）

2. **中優先級**：
   - Library 目錄下常用模組的 po 檔
   - 翻譯完成度 50-80% 的檔案

3. **低優先級**：
   - 翻譯完成度 < 50% 的大型檔案
   - 較少使用的進階主題檔案

### 3. 使用 AI 代理進行翻譯

#### 基本翻譯指令範例

```
請協助翻譯這個 Python 文件的 po 檔：[檔案路徑]

翻譯要求：
1. 目標語言：臺灣繁體中文 (zh_TW)
2. 保持 reStructuredText 格式
3. 專有名詞參考術語表
4. 遵循翻譯守則
```

#### 具體操作步驟

1. **分析翻譯狀態**：
   ```bash
   # 檢查整體進度
   make progress
   
   # 查看待翻譯條目
   make todo
   
   # 找出需要校閱的 fuzzy entries
   make fuzzy
   ```

2. **選擇翻譯目標**：根據分析結果自動選擇最適合的 po 檔

3. **開啟 po 檔**：使用 Poedit 或文字編輯器

4. **提供上下文**：將整個 po 檔或特定條目提供給 AI 代理

5. **指定翻譯規則**：明確說明翻譯要求和格式需求

6. **逐段翻譯**：建議分段處理，避免一次處理過多內容

### 4. 翻譯後處理

翻譯完成後，AI 代理應依序執行以下檢查：

1. **語法格式檢查**（必須）：
   ```bash
   make lint
   ```
   檢查 po 檔格式是否正確，確保沒有語法錯誤。

2. **編譯測試**：
   ```bash
   make build [檔案名稱].po
   ```
   編譯單一檔案，檢查是否有 reST 語法錯誤或警告。

3. **完整編譯**（可選）：
   ```bash
   make all
   ```
   編譯整個文件集，用於最終檢查。

4. **預覽結果**：
   ```bash
   cd ../cpython/Doc/build/html
   python3 -m http.server
   ```
   在瀏覽器中預覽翻譯結果，確認顯示效果。

**重要**：每次翻譯完成後必須執行 `make lint`，確保格式正確後才能提交。

## 翻譯品質控制

### 自動檢查項目

AI 代理翻譯時應注意：

1. **格式保持**：
   - 保留所有 `:func:`、`:class:`、`:mod:` 等 reST 語法
   - 維持超連結格式 `link text <target>`_
   - 保持程式碼區塊標記 `::` 

2. **標點符號**：
   - 中文使用全形標點：「」（）、，。
   - 英文使用半形標點
   - 中英文間適當加入空白

3. **專有名詞**：
   - 參考[術語列表](https://github.com/python/python-docs-zh-tw/wiki/%E8%A1%93%E8%AA%9E%E5%88%97%E8%A1%A8)
   - 高頻詞保留原文：`int`、`str`、`list`、`dict` 等

### 人工審核重點

1. **語意正確性**：確保翻譯符合原文意思
2. **上下文連貫**：檢查前後文的一致性
3. **技術準確性**：驗證技術術語翻譯正確
4. **流暢度**：確保中文表達自然流暢

## 注意事項與限制

### AI 代理的限制

1. **上下文理解**：可能無法完全理解複雜的技術概念
2. **術語一致性**：需要明確指導才能保持術語統一
3. **格式複雜性**：複雜的 reST 格式可能處理不當
4. **文化適應**：某些表達方式需要在地化調整

### 必須人工處理的情況

1. **專業術語爭議**：需要社群討論的翻譯選擇
2. **複雜格式**：包含多層巢狀的 reST 語法
3. **參考文獻**：需要查證的技術資料
4. **文化脈絡**：需要在地化的表達方式

## 最佳實踐

### 1. 分段處理

```
建議每次處理 10-20 個 msgstr，避免：
- 上下文丟失
- 錯誤累積
- 格式混亂
```

### 2. 提供充分上下文

給 AI 代理的指令應包含：
- 文件的整體主題
- 當前段落的技術背景
- 相關的術語定義
- 目標讀者群體

### 3. 迭代改進

AI 代理應遵循以下工作流程：

```
分析翻譯狀態 → 選擇翻譯目標 → 初稿翻譯 → 格式檢查(make lint) → 語意審核 → 術語統一 → 編譯測試 → 最終校對
```

每個步驟都必須通過才能進入下一階段。

### 4. 版本控制

每次翻譯完成後：
```bash
git add [檔案名稱].po
git commit -m "AI-assisted translation: [檔案描述]"
git push origin HEAD
```

### 5. 協作溝通

- 在 issue 中標註使用了 AI 輔助翻譯
- 請求社群協助審核 AI 翻譯的內容
- 在 PR 中說明 AI 使用情況

## 範例：翻譯單一條目

### 原文 (msgid)
```
The :func:`len` function returns the number of items in a container.
```

### AI 翻譯指令
```
請將以下 Python 文件片段翻譯為臺灣繁體中文，保持 reST 格式：

原文：The :func:`len` function returns the number of items in a container.

要求：
1. 保持 :func:`len` 格式不變
2. "container" 翻譯為"容器"
3. 使用自然的中文表達
```

### 翻譯結果 (msgstr)
```
:func:`len` 函式會回傳容器中項目的數量。
```

## 結語

AI 代理是強大的翻譯輔助工具，但不能完全取代人工審核。透過適當的使用和嚴格的品質控制，可以大幅提升翻譯效率和品質。

記住：**AI 輔助，人工把關**是確保翻譯品質的關鍵原則。

---

如有任何問題或建議，歡迎在 [GitHub Issues](https://github.com/python/python-docs-zh-tw/issues) 或 [Discord](https://discord.gg/44XheGXhWH) 中討論。